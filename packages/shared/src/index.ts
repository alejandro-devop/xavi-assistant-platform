/**
 * Shared contracts between the gateway and its clients.
 *
 * The real command/response types and the skill contract arrive with Phase 1
 * (see docs/ROADMAP.md); this stub only gives the workspace pipelines
 * something to typecheck until then.
 */
export const SHARED_PACKAGE = "@xavi/shared";
